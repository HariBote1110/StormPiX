{
  "name": "stormpix",
  "version": "0.1.0-Alpha-22e",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "test": "vitest run",
    "bench": "node --experimental-strip-types bench/run.ts",
    "bench:compare": "node --experimental-strip-types bench/compare/run.ts",
    "stormpix": "node --experimental-strip-types tools/stormpix.ts"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^7.1.5",
    "vitest": "^3.2.4"
  }
}
