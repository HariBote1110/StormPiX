# LZ復号器の局所最適化

## 決定

- 初回描画色は`nil`でも必ず更新されるため、色キャッシュの初期値を`-1`から`nil`へ縮める。
- tick比較を等価な大なり比較にし、ASCII値復号の減算式を短縮する。

## 計測

- `astral_opening` は8,111文字から8,106文字へ、5文字減った。

## 検証

- 正確性比較とLua往復再生で全30フレームのpixel一致を確認する。
