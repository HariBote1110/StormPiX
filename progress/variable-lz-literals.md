# 可変長LZリテラル

## Decision

- LZの仮リテラル頻度でpaletteを並べ替え、上位58色は1文字、残りは2文字で符号化する。
- 58文字の短縮領域と6個の2文字接頭辞で、最大442色まで可逆に表す。

## Measurement

- `astral_opening` は8,954文字から8,222文字へ732文字減った。

## Constraints / Gotchas

- 443色以上では可変長形式を使わず固定2文字へ戻す。
- LZ参照位置は色番号の置換で変わらないため、頻度順への並べ替えは参照構造を変えない。
